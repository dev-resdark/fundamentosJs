/**
 * Variables const
 */

const cliente = "Juan"
console.log(cliente)

const precioDescuento = 220
console.log(precioDescuento)

/**
 * IMPORTANTE
 * const es una sintaxis igual que let que sirve para crear variable pero, una diferencia es que una variable creada por const no puede ser reasignada
 * La siguiente diferencia es que una variable creada con const no puede iniciar sin un valor.
 */